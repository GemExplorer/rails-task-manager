<%= form_with model: @task, local: true do |form| %>
  <% if @task.persisted? %>
    <div>
      <%= form.label :completed %>
      <%= form.check_box :completed %>
    </div>
  <% end %>

  <%= form.submit %>
<% end %>
